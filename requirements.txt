# Used by Vercel (and anyone deploying from the repo root).
# Keep in step with backend/requirements.txt - local runs use that file.
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.5
opencv-python-headless>=4.9
numpy>=1.24
scipy>=1.11
