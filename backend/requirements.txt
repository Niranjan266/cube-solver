# --- core (required) ---------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.5
opencv-python-headless>=4.9
numpy>=1.24
scipy>=1.11            # optimal balanced colour assignment (pure-python fallback exists)

# The ~23-turn two-phase solver is built in - nothing to install for it.

# --- optional: YOLO sticker detection ---------------------------------------
# ultralytics>=8.1
# huggingface-hub>=0.23
